apiVersion: v1
kind: Service

metadata:
  name: eks-dev-service

spec:
  type: {{ .Values.service.type }}

  selector:
    app: eks-dev

  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
